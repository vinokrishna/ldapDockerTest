This project is to deploy MongoDB and LDAP together. To run the docker containers, use below command inside the directory that contains files:

  $ docker-compose up

To stop this ctl-c from the same command prompt or use below command 

  $ docker-compose stop

To remove the containers created with this:

  $ docker-compose rm
